module.exports = {
    //Roles available to be assigned to users
    roles: [
    ],

    //Resets the available roles on startup, removes all roles not found in this config file
    overwriteExistingRoles: false,

    //Resets role permissions on startup, removes any permissions not found in this config file
    overwriteExistingPermissions: true,

    //Default role assigned to all users
    defaultRole: "Participant"
};